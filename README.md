# Demo Site

A static site built with plain HTML, CSS and JavaScript. No build step.

## Local preview

```bash
python3 -m http.server 8000
```

Then open http://localhost:8000.

## Deploy

Pushes to `main` on GitHub deploy automatically to Netlify.
